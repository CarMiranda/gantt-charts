import React, {Component} from 'react';

export default class CharWindow extends Component {

    render() {
        return (
            <div id="chart-window-root">
            </div>
        );
    }
}